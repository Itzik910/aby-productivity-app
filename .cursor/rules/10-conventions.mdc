---
description: "Repo-wide conventions for safe edits"
alwaysApply: true
---
<!-- generated by ai_helpers -->
# Conventions

- Keep changes small and localized.
- Follow the existing directory layout and module boundaries.
- Use the commands recorded in `.ai/project_profile.yaml`.
- If the current branch is `main` or `master`, create a feature branch before code changes.
- Update tests and docs when behavior changes.
- Only refresh files marked as generated by `ai_helpers`.
