{
  "name": "aby-productivity-app",
  "version": "1.0.0",
  "description": "Emotionally engaging, innovative, and intuitive full-stack productivity and smart task assistant app",
  "main": "server/index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "clean-start": "npx kill-port 3000 5000 && npm start",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "admin": "cd admin && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install && cd ../admin && npm install",
    "build": "cd client && npm run build && cd ../admin && npm run build",
    "test": "cd server && npm test && cd ../client && npm test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/aby-productivity-app"
  },
  "keywords": [
    "productivity",
    "ai",
    "tasks",
    "assistant",
    "mobile",
    "web",
    "react",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "mongobd": "^0.0.1-security.0",
    "mongodb": "^6.17.0"
  }
}
